<!DOCTYPE html>
<html>
  <head>
    <title>BarHop</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <div class="container-fluid">
          <%= link_to 'BarHop', root_path, :class => 'navbar-brand' %>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <%= link_to 'Explore Bars', bars_path, :class => 'nav-link active' %>
              </li>
              <li class="nav-item">
                <%= link_to 'Sign up', new_user_path, :class => 'nav-link' %>
              </li>
              <li class="nav-item">
                <%# <%= link_to 'Sign in', new_session_path, :class => 'nav-link' %>
                <a class="nav-link" href="#" tabindex="-1">Sign in</a>
              </li>
            </ul>
            <%= form_tag bars_search_path, :class => 'd-flex' %>
            <%= text_field_tag :location, nil, :class => 'form-control me-2', :placeholder => 'Hawthorn, Victoria' %>
            <%= submit_tag "Search", :class => 'btn btn-outline-success' %>
          </div>
        </div>
      </div>
    </nav>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
